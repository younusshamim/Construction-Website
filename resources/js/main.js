//------- mixitup
$(document).ready(function () {
    var mixer = mixitup('.portfolioProjects');
    var mixer = mixitup(containerEl);

    var mixer = mixitup(containerEl, {
        selectors: {
            target: '.blog-item'
        },
        animation: {
            duration: 300
        }
    });
});

